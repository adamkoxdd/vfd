# VFD Shell

`vfdshell` is the primary desktop command palette. `vfdlauncher` remains as a compatibility symlink.

Recommended i3 binding:

    bindsym $mod+space exec --no-startup-id ~/.local/bin/vfdshell
