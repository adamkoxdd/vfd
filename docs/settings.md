# VFD Settings

`vfdsettings` is the first VFD Control Center. It provides keyboard-driven pages for Appearance, Components, Keybindings and About.

Theme selection is sent to `vfdd` with `THEME name` and persisted in `~/.config/vfd/config.ini`.
